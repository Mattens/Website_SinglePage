[

    //
    // keybindings.json in the .vscode directory is ignored.
    // %APPDATA%\Code\User\keybindings.json has to be edited.
    // Alternativly strg + K, strg + S can be used.
    // Serach: workbench.action.debug.restart => enter the desired key stroke(s)
    // 

    {
        "key": "f1",
        "command": "workbench.action.debug.restart",
        "when": "inDebugMode"
    }

]